spring:
  application:
    name: storesmanagement
  cloud:
#    config:
#      uri: ${config.server.uri:http://${HOST:config.ci.prod.auction.local}:8888}
#      label: backend
encrypt:
  failOnError: true
